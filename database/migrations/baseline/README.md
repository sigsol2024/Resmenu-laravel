# Baseline migrations (schema-baseline-v1)

**IMMUTABLE after freeze.** Do not edit files in this directory after the baseline tag.

Future schema changes belong in `database/migrations/` (incremental only).

Regenerate draft baselines only via `php database/scripts/generate_baseline.php` before a new freeze cycle.
