-- Platform reference: menu templates (idempotent by slug). Add rows via admin or extend this file.
SET NAMES utf8mb4;

-- Templates are environment-specific; seed only ensures table is usable on fresh installs.
-- Full template catalog should be synced from staging export when needed.
